services:
  - type: web
    name: sanal-ekonomi
    runtime: docker
    plan: free
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: DB_PATH
        value: /var/data/economy.db
    disk:
      name: economy-data
      mountPath: /var/data
      sizeGB: 1
